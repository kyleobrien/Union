{
    "Now": {
        "prefix": "now",
        "body": [
            "${CURRENT_YEAR:YYYY}-${CURRENT_MONTH:MM}-${CURRENT_DATE:DD} ${CURRENT_HOUR:hh}:${CURRENT_MINUTE:mm}"
        ],
        "description": "The current date and time.",
        "scope": "yaml"
    }
}
